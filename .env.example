# Vercel KV credentials (auto-populated when you link KV from Vercel dashboard)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Optional: Admin key to view dislike counts via API
REACTIONS_ADMIN_KEY=your-secret-admin-key
